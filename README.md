# SIMP UniMelb Website

- Vite + React
- Netlify deploy via `netlify.toml`
- Decap CMS at `/admin`

## Local

```bash
npm install
npm run dev
```

## CMS

- Go to `/admin` on the deployed site
- Login via Netlify Identity (invite-only recommended)

